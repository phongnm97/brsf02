<%= form_for(@category ,url: yield(:path)) do |f| %>
  <%= render "shared/error_messages", object: f.object %>

  <%= f.label t("model.category.name") %>
  <%= f.text_field :name, class: "form-control" %>
  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>
