  <% provide :title, t(".title") %>
<% if user_signed_in? %>
  <div class="container">
    <div class="col-sm-8">
    <%= render "shared/feed" %>
    </div>
    <%= render "comments/edit_form" %>
  </div>
<% else %>
  <div class="center jumbotron">
    <h1><%= t ".application" %></h1>
    <%= link_to t("layouts.Signupnow"), new_user_registration_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
