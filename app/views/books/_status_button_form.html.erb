<%= form_for(@book_status, remote: true) do |f| %>
  <div><%= hidden_field_tag :book_id, book.id %></div>
  <div><%= hidden_field_tag :status, status %></div>
  <%= f.submit button, class: "btn btn-primary btn-sm" %>
<% end %>
