<%= form_for(current_user.book_statuses.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :book_id, book.id %></div>
  <div><%= hidden_field_tag :followed_id, book.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
