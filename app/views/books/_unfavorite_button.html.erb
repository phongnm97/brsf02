<%= form_for(book_favorite,html: { method: :delete },
  remote: true) do |f| %>
  <%= button_tag(:submit, {class: "btn btn-link favorite-checked"}) do %>
    <i class="glyphicon glyphicon-heart"></i>
  <% end %>
<% end %>
