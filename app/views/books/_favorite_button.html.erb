<%= form_for(current_user.book_favorites.new, :html => { class: "star star-checked"}, remote: true) do |f| %>
   <div><%= hidden_field_tag :book_id, book.id %></div>
  <%= button_tag(:submit, {class: "star btn btn-link"}) do %>
      <i class='glyphicon glyphicon-heart'></i>
  <% end %>
<% end %>
